{
    "name": "le-clef-mot-root",
    "private": true,
    "scripts": {
        "vercel-build": "cd web && npm install && npm run build"
    },
    "dependencies": {
        "firebase": "^12.11.0"
    }
}
